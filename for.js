const numeros = [100, 200,300, 400, 500, 600]

// numeros.length = 6
for (i = 0; i < numeros.length; i ++) {

 // retorna índice 0 e índice 0 da const numeros e até concluir os 6 elementos   
    console.log(i, numeros[i])
}

